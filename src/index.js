import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // если нет – можешь временно опустить
import CareerPathApp from './App';

ReactDOM.render(
  <React.StrictMode>
    <CareerPathApp />
  </React.StrictMode>,
  document.getElementById('root')
);